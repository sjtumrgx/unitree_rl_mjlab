# Topology Get-Up Phase-13 Head-Contact Tuning Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Reduce premature episode death from head-contact now that lift/orientation shaping is stronger.

**Architecture:** Widen the tolerant head-contact grace window and persistence threshold in topology-getup config so the policy can continue a bit longer before the head-contact termination fires.
